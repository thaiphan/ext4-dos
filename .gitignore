# Build outputs
*.o
*.obj
*.exe
*.com
*.lib
*.err
*.lst
*.map
*.tmp
build/
dist/

# Vendored references (clones of lwext4 / grub for reading; not committed)
references/

# Heavy-debug DOSBox-X clone, populated by scripts/setup-debugger.sh
external/

# Test fixture images (regenerable from scripts/)
tests/images/*.img
tests/images/*.ext4
tests/images/*.raw
tests/images/*.expect

# FreeDOS test image + cached download (large, fetched on demand)
tests/freedos/

# MS-DOS 4 working copies — keep the source image, ignore the rest
tests/msdos4/*
!tests/msdos4/msdos4-source.img
!tests/msdos4/README.md

# MS-DOS 6.22 — proprietary, never committed; created by scripts/setup-msdos6.sh
tests/msdos6/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Claude Code (local user settings)
.claude/settings.local.json
